<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 注意：这里不要声明 READ/WRITE_EXTERNAL_STORAGE。app 模块声明它们时带了
         android:maxSdkVersion 限制，本模块若无限制地再声明一次，manifest 合并后
         会取并集而丢掉那个限制。READ_PHONE_STATE 同理不需要：
         getNetworkOperator/getSimOperator 都不要求该权限。 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--    Oppo push-->
    <uses-permission android:name="com.coloros.mcs.permission.RECIEVE_MCS_MESSAGE" />
    <uses-permission android:name="com.heytap.mcs.permission.RECIEVE_MCS_MESSAGE" />

    <!--    honor-->
    <queries>
        <intent>
            <action android:name="com.hihonor.push.action.BIND_PUSH_SERVICE" />
        </intent>
    </queries>

    <application>

        <!--huawei-->
        <service
            android:name=".huawei.HwPushService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.huawei.push.action.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--xiaomi-->
        <meta-data
            android:name="MIPUSH_APPID"
            android:value="${XIAOMI_APP_ID}" />
        <meta-data
            android:name="MIPUSH_APPKEY"
            android:value="${XIAOMI_APP_KEY}" />

        <receiver
            android:name="cn.wildfirechat.push.xiaomi.MiMessageReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.xiaomi.mipush.RECEIVE_MESSAGE" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.xiaomi.mipush.MESSAGE_ARRIVED" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.xiaomi.mipush.ERROR" />
            </intent-filter>
        </receiver>
        <!--        xiaomi end-->

        <!--vivo-->
        <service
            android:name="com.vivo.push.sdk.service.CommandClientService"
            android:exported="true" />
        <activity
            android:name="com.vivo.push.sdk.LinkProxyClientActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- push应用定义消息receiver声明 -->
        <receiver
            android:name="cn.wildfirechat.push.vivo.PushMessageReceiverImpl"
            android:exported="true">
            <intent-filter>
                <!-- 接收push消息 -->
                <action android:name="com.vivo.pushclient.action.RECEIVE" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.vivo.push.api_key"
            android:value="${VIVO_APP_KEY}" />
        <meta-data
            android:name="com.vivo.push.app_id"
            android:value="${VIVO_APP_ID}" />

        <!--        Oppo push-->
        <service
            android:name="com.heytap.mcssdk.PushService"
            android:exported="true"
            android:permission="com.coloros.mcs.permission.SEND_MCS_MESSAGE">
            <intent-filter>
                <action android:name="com.coloros.mcs.action.RECEIVE_MCS_MESSAGE" />
            </intent-filter>
        </service>

        <service
            android:name="com.heytap.mcssdk.AppPushService"
            android:exported="true"
            android:permission="com.heytap.mcs.permission.SEND_MCS_MESSAGE">
            <intent-filter>
                <action android:name="com.heytap.mcs.action.RECEIVE_MCS_MESSAGE" />
            </intent-filter>
        </service>

        <meta-data
            android:name="OPPO_APP_PUSH_KEY"
            android:value="${OPPO_APP_KEY}" />
        <meta-data
            android:name="OPPO_APP_PUSH_SECRET"
            android:value="${OPPO_APP_SECRET}" />

        <!--honor-->
        <meta-data
            android:name="com.hihonor.push.app_id"
            android:value="${HONOR_APP_ID}" />
        <service
            android:name="cn.wildfirechat.push.honor.HonorPushService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.hihonor.push.action.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--        Google  -->
        <service
            android:name="cn.wildfirechat.push.firebase.MyFirebaseMessagingService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />

    </application>

</manifest>
