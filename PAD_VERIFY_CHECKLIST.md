# Pad 适配验证清单（P0–P9）

P0–P4 已落地为六个提交（`4cf662e3` → `1e0e1525`，含中途那个 opener 失效的 bugfix
`6eaf53a6`）。P5 的代码改动尚未提交。

清单分两类：**［回归］** 是「必须和改动前一模一样」，**［新］** 是「必须生效」。
回归项是这轮的红线，优先验。

配套文档：适配方案与分阶段计划见 [PAD_ADAPTATION_PLAN.md](PAD_ADAPTATION_PLAN.md)。

共 139 项：P0 10 项 · P1 26 项 · P2 20 项 · P3 9 项 · P4 14 项 · P5 15 项 · P6 18 项 ·
P7 11 项 · P8 7 项 · P9 9 项。另有 3 项「需要真机才能判的」附在末尾，一并勾则是 142 项。

P5 是**验证阶段**：它自己只有 4 项新回归（一次让分流可被单测的抽取），
其余 11 项是**设备矩阵** —— 拿哪几台设备、每台跑哪几轮，把前面 P0–P4 的项覆盖完。

---

## 开始之前

- P0 改了 iOS/Android **原生代码**，热重载与热重启都不会重编 —— 必须重新
  `flutter run`，否则设备形态一律回落 `unknown`，平板仍按手机跑，P2/P3 的平板项全验不到。
- **鸿蒙平板会立即变成两栏**：ohos 的 `getDeviceType` 早就返回 `tablet`，不需要重编原生，
  P1 一合入形态就变了。这是三种 pad 里唯一「不重编也会变」的。
- 平台号 8/9 服务端已确认支持；若测试环境未同步，P0 的登录会失败。

---

## P0 · 设备形态识别与协议平台号（10 项）

改动面：`imclient/lib/imclient_platform.dart` · `imclient/ios/Classes/ImclientPlugin.m` ·
`imclient/android/src/main/java/cn/wildfirechat/imclient/ImclientPlugin.java`。

平台号在登录时一次性上报给 app server，看服务端日志或管理后台的「在线设备」最直接。

| 设备 | 改前 | 改后 | 性质 |
|---|---|---|---|
| iPhone | 1 | 1 | 回归 |
| Android 手机 | 2 | 2 | 回归 |
| iPad | 1 | **8** | 新 |
| Android 平板 | 2 | **9** | 新 |
| 鸿蒙手机 / 平板 / 电脑 | 10 / 11 / 12 | 10 / 11 / 12 | 回归 |
| Windows / macOS / Linux | 3 / 4 / 7 | 3 / 4 / 7 | 回归 |

### 平台号上报

- [ ] ［新］**iPad** —— 登录后服务端记录的平台号是 **8**（iPad），不是 1
- [ ] ［新］**Android 平板** —— 平台号是 **9**（APad），不是 2
- [ ] ［回归］**手机 · PC** —— iPhone / Android 手机 / PC 三端平台号**逐位未变**（1 / 2 / 3·4·7）
- [ ] ［回归］**鸿蒙** —— 鸿蒙手机 10、鸿蒙平板 11、鸿蒙电脑 12 未变

### 服务端连带影响

- [ ] ［新］**平板** —— iPad / Android 平板**能正常登录**（服务端认 8/9，不会被拒）
- [ ] ［新］**平板** —— **离线推送能收到**（平台号参与推送目标选择，见 `PUSH_PLAN.md`）
- [ ] ［新］**iPhone + iPad** —— 同时登录同一账号，互踢/静音行为符合预期。
      **这是行为变化**：以前 iPad 报 1 与 iPhone 同类，现在报 8 是不同类端

### 兜底与稳定性

- [ ] ［回归］**平板** —— **不重编原生**直接跑：不崩、不报错，平板回落成手机行为（平台号 1/2、单栏 UI）
- [ ] ［新］**Android 平板** —— **分屏到 1/3 宽**后重新登录，平台号仍是 9
      （判定取的是设备全局配置，不随窗口抖动）
- [ ] ［回归］**手机** —— 启动耗时无明显变化（iOS/Android 现在会多一次 `getDeviceType` 通道往返）

---

## P1 · 拆轴（纯回归，全程红线）（26 项）

250 处判断点重新分流，**但三条轴与兜底桶当前全部求值为 `WfcPlatform.isDesktop`**，
理论上零变化。这一节没有「新行为」，只有「有没有哪里变了」。下面按改动最密集的文件排优先级。

### 手机端 · 高风险优先（16 项）

- [ ] ［回归］**会话列表** —— 行高、头像尺寸、时间与未读徽标位置、置顶行底色、分隔线缩进
      （`conversation_list_widget`，18 处）
- [ ] ［回归］会话列表**左滑菜单**（iOS/鸿蒙）与**长按菜单**位置（菜单向左偏移、项高 48）
- [ ] ［回归］「**PC 已登录**」横幅仍会出现（这条走的是「本机是不是 PC」，不是 UI 轴）
- [ ] ［回归］**消息气泡** —— 宽度、收发两色、引用条、时间标签、发送者名
      （`portrait_cell_builder`，12 处）
- [ ] ［回归］**长按气泡**出菜单、长按全选后拖手柄调整选区（桌面端不挂长按，别在手机上被误伤）
- [ ] ［回归］**输入栏** —— 表情面板、插件面板里**相机 / 位置 / 通话**三项都在（桌面端才隐藏）、录音
- [ ] ［回归］输入框键入 `@` **跳选人页**（桌面端才是就地浮层）
- [ ] ［回归］**媒体预览** —— 滑动翻页、双指缩放、拖拽下滑关闭、底部控制条位置
      （`mm_preview_view`，12 处）
- [ ] ［回归］**通讯录 / 发现 / 工作台 / 我** —— 分组留白、分隔线缩进 32、右箭头样式、开关行
      （`contact_list_widget` 10 处、`option_item`）
- [ ] ［回归］**组织架构** —— 搜索框圆角与填充色、行高 56/72（`organization_screen`，12 处）
- [ ] ［回归］**群/单聊/频道资料页** —— AppBar、背景色、成员宫格**每行 5 个**
- [ ] ［回归］**通话与会议** —— 单人、多人、会议三套布局，最小化悬浮窗
      （`conference_call_screen`，10 处）
- [ ] ［回归］点消息里的**链接**，用**内嵌 WebView** 打开（不是跳系统浏览器）
- [ ] ［回归］**Toast** 仍是系统原生样式（Fluttertoast），不是桌面浮层
- [ ] ［回归］**按钮尺寸与圆角** —— 主按钮、大按钮的高度/内边距/字号（`app_theme`，10 处）
- [ ] ［回归］**登录 → 登出 → 被踢 → 重新登录**全链路正常

### PC 端 · 高风险优先（10 项）

- [ ] ［回归］**三栏 Shell** —— 侧栏 tab、中栏宽度可拖、输入栏可拖高、拖拽尺寸重启后恢复
- [ ] ［回归］**指针交互** —— hover 高亮、鼠标指针形状、右键菜单（会话行、气泡、头像）、选中会话常驻高亮
- [ ] ［回归］**快捷键** —— 搜索、方向键切会话、Esc 退多选、Cmd/Ctrl+C 复制气泡选区、Enter 发送
- [ ] ［回归］**拖拽文件**进会话区发送；滚轮滚动无回弹；滚到顶自动加载更早消息
- [ ] ［回归］**独立子窗口** —— 媒体预览、朋友圈、搜索、通话，四种都能开且能关
- [ ] ［回归］**原生能力** —— 托盘图标与未读、截图、开机自启、窗口尺寸记忆
- [ ] ［回归］**二维码登录**页仍是固定小窗，登录后放大回上次窗口尺寸
- [ ] ［回归］收藏、文件记录、工作台页签、投票、网盘 —— **桌面弹窗**形态（不是整页 push）
- [ ] ［回归］点消息里的链接，走**系统默认浏览器**（不是内嵌页）
- [ ] ［回归］备份/恢复 —— 备份包 appType 仍是 **PC**；手机端仍是 MOBILE

---

## P2 · 平板两栏 Shell（20 项）

断点 `720` 逻辑像素。左栏固定 320，右栏是嵌套 Navigator。
手机与 PC 在这一节只需确认「多包了一层 `AppHome` 但行为没变」。

### 断点与形态切换（7 项）

- [ ] ［新］**平板** —— iPad **横屏**、以及竖屏（≥720）→ **两栏**：左列表 + 右详情
- [ ] ［新］**iPad** —— **1/3 分屏**（<720）→ 回落成原来的单栏 `HomeTabBar`
- [ ] ［新］**Android 平板** —— **分屏**拖动分割线跨过断点，两种形态来回切不崩
- [ ] ［新］**平板** —— **宽 → 窄**旋转：正在看的会话变成整页，聊天上下文不丢
- [ ] ［新］**平板** —— **窄 → 宽**旋转：整页会话自动收起，右栏接着显示同一个会话
- [ ] ［新］**平板** —— 停在**设置页/资料页**时旋转，页面**不被弹掉**（只有会话页会被收起）
- [ ] ［新］**平板** —— 连点几个会话再旋转成宽栏，叠起来的多层会话页**一次性收干净**

### 右栏行为（8 项）

- [ ] ［新］**平板** —— **点会话列表 → 右栏显示会话页**（这是刚修的那个 bug，重点验）
- [ ] ［新］**平板** —— 左栏会话列表里，**当前会话高亮**（移动端选中底色，不是 PC 那个）
- [ ] ［新］**平板** —— 右栏会话页的 AppBar **没有返回键**（栏内没有上一页可回）
- [ ] ［新］**平板** —— 右栏点「更多」进群资料 → **有**返回键，返回回到会话页
- [ ] ［新］**Android · 鸿蒙平板** —— **系统返回键**：在右栏子页按返回 → 回到会话页，**不退出 App**
- [ ] ［新］**平板** —— 未选中任何会话时，右栏是**淡化的 App 图标**空态
- [ ] ［新］**平板** —— 从通讯录点用户 → **右栏**显示用户资料（走 app_navigator 的页面都该进右栏）
- [ ] ［新］**平板** —— 左栏五个 tab 都能切，底部栏、搜索、加号菜单在 320 宽下不挤爆

### 登录态与实例数（3 项）

- [ ] ［新］**平板** —— **走登录页登入** → 两栏可用，点会话有反应（bugfix 的正面用例）
- [ ] ［新］**平板** —— **已登录直接启动** → 两栏可用，点会话有反应
- [ ] ［新］**平板** —— **登出后换账号登录** → 右栏不残留上个账号的会话

### 手机 / PC 回归（2 项）

- [ ] ［回归］**手机** —— 登录后仍进 `HomeTabBar`，行为与改前一致（中间多了一层 `AppHome`）
- [ ] ［回归］**PC** —— home 变成 `AppHome → PCHome`，三栏与右栏导航行为一致；
      **扫码登录后右栏点设置项能正常切换**（这正是当年 PC 踩过、这次平板又踩一次的 opener 失效症状）

---

## P3 · 面板宽度正确性（9 项）

只动了三处。`articles` / `link` / `card` / `rich_notification` 四个 cell_builder **刻意没改** ——
实测它们不会溢出（Flutter 约束系统兜底 + 各自的 `.clamp()`），改了对三端都是 no-op。

### 气泡最大宽度（5 项）

- [ ] ［新］**平板** —— 两栏下发一条**超长文本**：气泡**不横贯整个右栏**，右侧有留白
- [ ] ［新］**平板** —— **引用消息**的引用行与气泡**同一个宽度上限**，不各自漂移
- [ ] ［新］**平板** —— 窄栏（分屏）下气泡**不设上限**，和手机一样铺满
- [ ] ［回归］**PC** —— 长文本气泡最大宽度**与改动前完全一致**（内容区宽 − 120）
- [ ] ［回归］**手机** —— 气泡宽度**无变化**，长文本仍由外层收着

### 文件消息（2 项）

- [ ] ［回归］**手机 · PC** —— 长文件名的截断位置**与改动前一致**（窗口宽 ÷ 3）
- [ ] ［新］**PC** —— **拖动窗口改变大小**，文件名可见宽度**跟着更新**（以前不更新，这是修好的）

### 录音遮罩（2 项）

- [ ] ［回归］**手机** —— 长按录音，遮罩**全屏**，与改动前一致
- [ ] ［新］**平板** —— 两栏下长按录音，遮罩**只盖右栏**，不糊到左栏会话列表上

---

## P4 · 平板专属体验（14 项）

改动面：`message_input_bar`（键盘高度按方向存）· `login_screen`（居中）·
`wf_asset_picker_delegate`（列数）· `pad_home` + `app_home`（会话删除清栏、旋转保状态）。

### 键盘高度按方向缓存（4 项）

存储从一个 key 拆成横竖屏两个，旧 key 作为升级兜底。

- [ ] ［回归］**手机** —— 升级后**第一次**打开表情/插件面板，高度与升级前一致（读旧 key 兜底）
- [ ] ［回归］**手机** —— 竖屏反复开合表情面板、切换键盘↔面板，无跳变（这块逻辑很敏感，多试几次）
- [ ] ［新］**手机 · 平板** —— 竖屏用一次键盘 → 转横屏 → 开表情面板，**直接就是横屏高度**，不再先弹错高度再纠正
- [ ] ［新］**平板** —— 横竖屏来回转多次，面板高度各自记住，不互相污染

### 登录页（2 项）

- [ ] ［新］**平板** —— 登录页表单**居中且限宽 420**，不再拉成整屏宽的横条
- [ ] ［回归］**手机** —— 登录页布局**无变化**（仍铺满）

### 相册选择器（2 项）

- [ ] ［新］**平板** —— 应用内相册每行 **6 张**（手机是 4 张）
- [ ] ［回归］**手机** —— 相册仍是每行 **4 张**，多选、单选、拍照入口都正常

### 右栏内容失效（3 项）

- [ ] ［新］**平板** —— 右栏正显示某会话时，从左栏**删除该会话** → 右栏自动回到空态，不停留在已删除的会话里
- [ ] ［新］**平板** —— 删除的是**别的**会话时，右栏当前会话**不受影响**
- [ ] ［新］**平板** —— **新建一个还没发过消息的会话**（不在列表里），右栏不会被立刻清掉

### 旋转保持左栏状态（3 项）

左栏 `HomeTabBar` 挂了 GlobalKey，跨断点时整棵子树连同 State 一起搬移。

- [ ] ［新］**平板** —— 停在**通讯录 tab** 时旋转跨断点，回来还在通讯录 tab，不跳回消息
- [ ] ［新］**平板** —— 会话列表**滚到中间**后旋转跨断点，滚动位置保持
- [ ] ［新］**平板** —— 反复旋转跨断点多次，左栏不白屏、不报 GlobalKey 冲突

---

## P5 · 验证阶段（15 项）

改动面：`app_shell`（抽出 `shellFor` + `AppShellKind`）· `app_home`（改用它）·
`portrait_cell_builder`（气泡宽度公式抽成纯函数）· 删掉 P1 拆轴后没人再用的
`pc/pc_platform.dart`（`isDesktopShell`）· 补单测。

### 本阶段改动回归（4 项）

三处都是「取值不变、只是换了个位置」，但其中两处落在 PC 与手机的主路径上，各看一眼。

- [ ] ［回归］**PC** —— 登录后仍进三栏 `PCHome`。
      分流判据从「是不是桌面」换成了「是不是平板」——
      真机上两者取值必然相同（桌面三端不查设备形态恒为 `unknown`，鸿蒙电脑是 `pc` 不是 `tablet`），
      但这是 PC 主路径上的改动
- [ ] ［回归］**手机** —— 登录后仍进单栏 `HomeTabBar`
- [ ] ［新］**平板** —— 宽栏仍进两栏、窄栏仍回落单栏，断点行为与 P2 验过的一致
- [ ] ［回归］**手机 · PC · 平板** —— 长文本气泡宽度与 P3 验过的一致（公式只是被抽成了纯函数）

### 设备矩阵（8 项）

每台设备至少跑一遍主链路：**登录 → 收发文本/图片/语音 → 开一次媒体预览 → 拨一次通话 → 登出**。
勾一行代表「这台设备上，前面 P0–P4 里适用的项都过了」。

- [ ] **iPad · 竖屏全屏**（≥720 → 两栏）
- [ ] **iPad · 横屏全屏**
- [ ] **iPad · 1:1 分屏**（约半宽，仍 ≥720 → 保持两栏）
- [ ] **iPad · 1:3 分屏**（约 320，<720 → 回落单栏）
- [ ] **iPad · 台前调度**：自由拖窗口尺寸**反复跨断点**，两种形态来回切不崩、会话不丢
- [ ] **Android 平板 · 竖屏 + 横屏**
- [ ] **Android 平板 · 分屏**：拖分割线跨断点
- [ ] **鸿蒙平板 · 竖屏 + 横屏**（提醒：这台**不重编原生**也已经是两栏了）

### 折叠屏（3 项）

折叠屏是这套判定里最不确定的一档，判据取的是设备全局 `smallestScreenWidthDp ≥ 600`。

- [ ] **展开**（内屏，宽 ≥720）→ 两栏；**折叠**（外屏，窄）→ 单栏
- [ ] **展开 ↔ 折叠**来回切多次：不崩、正在看的会话不丢（走的是与旋转同一条断点跨越路径）
- [ ] ⚠️ **平台号**：展开态启动时形态判为 `tablet`，上报的是 **9（APad）而不是 2**，
      且**折叠后不会改**（形态只在启动时查一次）。确认服务端的互踢/推送**能接受**这个行为；
      不能接受的话告诉我，判据要改成「不把折叠屏算平板」

---

## P6 · 各 tab 的下钻页进右栏（18 项）

改动面：`pad_home`（每 tab 一条独立详情栈）· `home/home`（搜索/加号入口、工作台左栏、
上报当前 tab）· `contact_list_widget` + `discovery_tab`（下钻改走 app_navigator）·
`popup_menu_overlay` + `selectable_message_text` + `conversation_controller`（菜单定位）·
新增 `pad/pad_workspace_welcome`。

导航形态参考 hm-chat 的 `SplitTabPane`：**页面在哪个 tab 打开，就留在哪个 tab 的栈里**。

### 左栏顶部入口（4 项）

- [ ] ［新］**平板** —— 点**搜索**：搜索页出现在**右栏**，不再盖住整屏
- [ ] ［新］**平板** —— 加号 →**发起聊天**：选人页在右栏；选完 1 人 → 右栏变会话，
      左栏列表**高亮**该会话；选多人 → 建群转圈结束后右栏变群聊
- [ ] ［新］**平板** —— 加号 →**添加朋友**：搜索用户页在右栏
- [ ] ［回归］**手机** —— 搜索、发起聊天、添加朋友、建群全链路**与改动前一致**
      （建群的「正在建群」转圈能正常消失，这处关闭逻辑动过）

### 通讯录 / 发现（4 项）

- [ ] ［新］**平板** —— 通讯录点**联系人**→ 右栏出资料页；新的朋友、群聊、频道、
      域名列表、**组织架构**同样进右栏
- [ ] ［新］**平板** —— 发现页六个入口（朋友圈、聊天室、机器人、会议、开发文档、网盘）
      全部进右栏；点**机器人**进的是会话（走 openConversation，左栏会高亮）
- [ ] ［回归］**手机** —— 通讯录与发现页所有入口**仍是整页跳转**，返回行为不变
- [ ] ［回归］**PC** —— 通讯录与发现页不受影响（PC 用的是自己的 `pc_contact_list` /
      `pc_discovery_list`，压根不经过改动的这两个文件）

### 每 tab 独立详情栈（4 项）

这是本阶段的形态核心，**重点验**。

- [ ] ［新］**平板** —— 在**通讯录**点开某人 → 切到**消息** tab 看会话 → 切回通讯录，
      **那个人的资料页还在**（不是变成刚才的会话）
- [ ] ［新］**平板** —— 从通讯录某人点「**发消息**」：会话开在**通讯录**这一栏，
      按返回能回到那个人的资料页（不是被甩到消息 tab）
- [ ] ［新］**Android · 鸿蒙平板** —— **系统返回键**只作用于**当前露面**的那一栏；
      在别的 tab 里下钻过好几层，也不会被这一次返回连带弹掉
- [ ] ［新］**平板** —— 右栏什么都没下钻时按返回 → 正常退出/回到桌面（不是卡住）

### 工作台（3 项）

- [ ] ［新］**平板** —— 工作台 tab：左栏是**问候语 + 日期**的欢迎面板，
      右栏是**真正的工作台网页**
- [ ] ［新］**平板** —— **没进过工作台 tab 之前**，工作台网页不加载
      （懒建；抓包或看服务端访问日志最直接）
- [ ] ［回归］**手机 · PC** —— 工作台形态无变化（手机整页网页、PC 右栏多页签）；
      平板上**旋转跨断点**时工作台在左右之间搬家，网页不白屏、不重复加载

### 消息长按菜单定位（3 项）

- [ ] ［新］**平板** —— 两栏下**长按消息气泡**：菜单贴着气泡弹出，
      **不再整体右移一个左栏的宽度**
- [ ] ［新］**平板** —— 长按选中正文后的**选区菜单**同样贴着选区；
      贴着右栏右边缘的气泡，菜单**不越出右栏**
- [ ] ［回归］**手机 · PC** —— 消息菜单与选区菜单位置**与改动前逐位相同**
      （手机上 Overlay 就是整屏，换算前后同值；PC 走的是另一条右键菜单路径）

---

## P7 · 两栏形态的三处收口（11 项）

改动面：`conversation_list_widget` + `pc_online_devices_screen`（横幅入口与刷新时机）·
`pad_home`（键盘 inset）· `app_navigator`（`pushFullScreen`、`openPage` 返回 Future）·
`conversation_controller` + `quoted_message_preview` + `conversation_media_grid_screen`（预览全屏）。

### PC 在线设备（3 项）

- [ ] ［新］**平板** —— 点会话列表顶部的「**PC 已登录**」横幅 → 设备页出现在**右栏**
- [ ] ［新］**平板** —— 在右栏把某台 PC **踢下线** → 左栏顶部的横幅**立刻更新**
      （少一台就少一台，全踢完横幅消失）。这条以前靠「页面被弹回」触发，右栏没有那一刻
- [ ] ［回归］**手机** —— 横幅点击、踢下线、免打扰开关**与改动前一致**

### 软键盘（5 项）

根因是 PadHome 那层 Scaffold 把两栏一起缩短了，现在改成两栏各自处理。

- [ ] ［新］**平板** —— 在右栏会话输入框打字：**左栏底部的 tab 栏不动**，
      左栏列表也不被挤扁（键盘只是盖在左栏下面那一截上）
- [ ] ［新］**平板** —— 键盘弹起时，左栏 tab 栏**不会掉到屏幕最底下**
      （home indicator 那一档安全区还在）
- [ ] ［新］**平板** —— 右栏输入栏**正常被键盘顶起**，输入框不被遮住
- [ ] ［新］**平板** —— 右栏的**表情/插件面板高度**正确（这块以前在平板上读不到
      键盘高度，P4 的按方向缓存等于是废的，现在才真正生效）：
      开键盘 → 切表情面板，高度一致不跳变；横竖屏各试一次
- [ ] ［回归］**手机 · PC** —— 键盘与面板行为**无变化**

### 媒体预览（3 项）

- [ ] ［新］**平板** —— 点消息里的图片/视频：预览**盖住整个窗口**，不是只占右栏
- [ ] ［新］**平板** —— 预览的**进出场动画**从气泡缩放展开、拖拽下滑退回气泡，位置对得上
      （以前压在右栏里连动画起点都是偏的）；引用消息里的图片、聊天媒体网格同样
- [ ] ［回归］**手机 · PC** —— 预览行为无变化（手机整页、PC 独立子窗口）

---

## P8 · 右栏栈的层级（7 项）

改动面：`pad_home`。P6 里「换内容」和「往下钻」共用了同一条清栈的路径，
现在分开：**栈顶已经是会话 → 替换；否则压入**。

- [ ] ［新］**平板** —— 通讯录点开某人 → 点「**发送消息**」→ 会话页**左上角有返回键**，
      按下去回到那个人的资料页（这是报的那个 bug）
- [ ] ［新］**平板** —— 消息 tab 直接点开的会话**没有**返回键（它下面只有空态，
      返回过去是一片空白）
- [ ] ［新］**平板** —— 在消息列表里连点好几个会话：右栏一直只有一层，
      按返回键**不会一层层退出**之前点过的会话
- [ ] ［新］**平板** —— 在通讯录点另一个联系人：右栏**换成**新的资料页，
      不是叠在上一个资料页上面
- [ ] ［新］**平板** —— 通讯录里 [资料页 → 会话] 时，那个会话**被删除** →
      只弹掉会话页，**露出下面的资料页**（不是整栏清空）
- [ ] ［新］**平板** —— 在通讯录栏按返回键，**不会**把消息 tab 里开着的会话取消高亮
      （五条栈各自独立）
- [ ] ［新］**平板** —— 会话页被返回键弹掉后，左栏列表的**高亮同步撤掉**

## P9 · 右栏的坐标系与层级（9 项）

改动面：`popup_menu_overlay`、`selectable_message_text`、`app_navigator`。
两个都只在平板上犯：菜单挂错了坐标系，`openPage` 分不清「换内容」和「往下钻」。

- [ ] ［新］**平板** —— 会话里**长按文本消息**：菜单在气泡**正上方对齐**，
      横向不再左偏一个左栏的宽度（这是报的那个 bug）
- [ ] ［新］**平板** —— 长按**贴着右栏左边缘**的收到消息（短文本，如「好」）：
      菜单被推回右栏内，**不骑到左栏上**
- [ ] ［回归］**平板** —— 长按**图片/文件**消息（走整屏遮罩那条路径）：菜单位置照旧正确
- [ ] ［回归］**手机** —— 长按文本、长按图片：菜单位置与改动前一模一样
- [ ] ［回归］**PC** —— 右键消息：仍是竖排上下文菜单，位置照旧
- [ ] ［新］**平板** —— 群会话 → 右上角进**群会话详情** → 点某个成员进**用户详情** →
      按返回：回到**群会话详情**，再按回到**群会话**（这是报的那个 bug）
- [ ] ［新］**平板** —— 「我」tab → 通用 → **隐私设置**：压在右栏同一条栈上，
      返回回得到通用设置页
- [ ] ［回归］**平板** —— 从**左栏**打开的页面（通讯录点某人、发现页某项、
      会话列表顶部 PC 在线状态）仍是**整栏替换**，不叠在上一个上面
- [ ] ［回归］**PC** —— 右栏内点设置项（如「通过什么找到我」）仍是**整栏替换**，
      转场依旧是无动画

---

## 需要真机才能判的（本轮未写代码）

这几项我没有设备无法判断，验的时候如果表现不好，告诉我具体现象我再改：

- [ ] 平板**横屏通话**：单人 `voip_call_screen`、多人、会议三套布局在宽屏下是否变形
- [ ] iPad **台前调度 / 分屏**改变窗口尺寸时，IM 连接**不重连**（不该出现「连接中」横幅）
- [ ] iPad 外接**键盘/触控板**时的表现（当前平板按纯触摸设计，快捷键未开）

---

## 我已经跑过的，不用手验

- `flutter analyze`：0 error，问题总数 **363**，与适配前基线逐条一致
  （说明没引入新告警，也没残留未使用 import）。
- `chat` `flutter test`：**106 通过，全绿**（P9 新增 8 项）。P5 顺手删掉了那个一直红着的 Flutter 模板残留
  `widget_test.dart`（它找的 `'Running on:'` 在 `lib/` 出现 0 次，且 bare-pump `MyApp()`
  缺 Provider 必抛）—— 留着它，套件就永远不能拿「全绿」当门槛。
- `imclient` `flutter test`：**16 通过，全绿**。
- 单测已锁定（P5 补齐）：
  - `clientPlatformCodeFor` 全映射表，含形态 `unknown` 时回落手机号段 1/2/10；
  - `parseDeviceType` 五种取值 + 未知兜底；
  - `AppShell.isMultiPane` 断点两侧、`shellFor` 三种 Shell 的分流；
  - **拆轴的零变化不变量**：非平板上三条轴与 `WfcPlatform.isDesktop` 恒等，
    平板不掺和密度轴/交互轴、也不让窗口宽影响它们，非平板任何宽度都拿不到平板两栏；
  - 气泡宽度公式三档取值、够宽时收敛回 PC 那一档、以及三档都随内容区单调不减。
- 单测已锁定（P9 补齐，`chat/test/pad_pane_navigation_test.dart`）：
  - 菜单锚点在**两种 Overlay** 下的换算（挂右栏 → 减掉左栏；挂根 Overlay → 保持全局，
    但可用范围仍限在右栏），以及**没有嵌套栏时两条路径逐位相同**——手机/PC 的零变化保证；
  - 菜单避让：贴右栏左边缘时不越到左栏，贴右边缘时推回范围内；
  - `openPage` 三种分流：平板栏内**压栈**（返回回得去）、栏外交给 Shell 整栏替换、
    PC 栏内仍是整栏替换。
- **单测覆盖不到的**：`isDesktopStyle` / `isPointerInput` 在真平板上为 false 这半句
  —— 测试宿主是桌面，`isDesktop` 是真正的平台常量顶替不了。单测锁的是
  「这两条轴的取值仍旧只由 `isDesktop` 决定」，剩下半句由 imclient 侧
  「设备形态不影响 isMobile/isDesktop 分流」补上，两边合起来才是完整命题。

## 已知缺口，本轮不用验

- P6 收掉了左栏四个 tab 的下钻入口，但**更深的层级仍有 64 处裸 `Navigator.push`**
  （会话页内 12、设置/「我」8、搜索结果 6、备份 5、投票 4、网盘 4…）。
  它们大多是「已经在右栏里，再往下钻一层」——`Navigator.push` 落到右栏那条栈上本来就对，
  真正需要改的是从**左栏**出发的那些。
- **「我」tab 刚进去时右栏是空态**。设置项本身会开进右栏（走 `openPage`，P9 起栏内再下钻
  也压在同一条栈上），缺的是「还没点任何一项」时右栏放什么 —— PC 那边默认是通用设置详情，
  平板还没编排。它整页都是设置项，没有天然的「列表 + 详情」层次，要做得像工作台那样重排一遍。
- 全局打开会话（通知点击等）走的是 `openConversation`，会开在**当前 tab** 那一栏。
  hm-chat 对这类「外部来的」会话会先切回消息 tab，我们还没区分来源。
- 左栏宽度固定 320 不可拖拽；≥900 时的 NavigationRail 未做。
- 「会话被删除清空右栏」这段逻辑 PadHome 与 PCHome **各有一份**。没有合并是因为抽公共实现要改
  PCHome，会碰 PC 红线；等下次有理由动 PCHome 时再统一。
- 平板未启用键盘快捷键。`AmbientShortcuts` 挂在 `isPointerInput` 上，平板取 false ——
  iPad 外接键盘属于「额外能力」而不是默认交互模型，要做得先设计「检测到硬件键盘才开」的门控。
- **设备形态只在启动时查一次**（`WfcPlatform.init()` 里一次通道往返后缓存）。
  折叠屏展开↔折叠不会改 `isTablet`，因而也不会改上报的平台号 —— UI 布局不受影响
  （那一档看的是窗口宽度，实时的），受影响的只有平台号。见 P5 折叠屏那一项。
- `chat/test/` 整个目录**未纳入 git**（不是 ignore，是从没 add 过），所以 P0–P4 那六个提交里
  不含任何测试。P5 新增/改动的测试同样只在工作区 —— 要让它们进 CI 得先 `git add chat/test`。
